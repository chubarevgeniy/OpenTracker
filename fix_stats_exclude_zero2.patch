--- calorie-tracker/src/pages/Stats.tsx
+++ calorie-tracker/src/pages/Stats.tsx
@@ -62,7 +62,7 @@
       data.push({
         date: format(date, 'MMM dd'),
         fullDate: dateStr,
-        calories: Math.round(calories),
+        calories: excludeZeroes && calories === 0 ? null : Math.round(calories),
         weight: log?.weight || null,
         dateObj: date,
         goalWeight: settings.weightGoal && dateStr >= settings.weightGoal.startDate && dateStr <= settings.weightGoal.targetDate
